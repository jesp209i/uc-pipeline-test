name: pipeline

on:
  push:
    branches: [ master ]
    
env:
  PATH_TO_CSPROJ: src/UmbracoProject/UmbracoProject.csproj
  PROJECT_ALIAS: jams-cicd-project
  APIKEY: {{ secrets.API_KEY }}
      
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Setup .Net Core
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '7.0.x'
      - name: Install Dependencies
        run: dotnet restore $PATH_TO_CSPROJ
      - name: Build
        run: dotnet build $PATH_TO_CSPROJ --configuration Release --no-restore
      - name: Test
        run: dotnet test $PATH_TO_CSPROJ --no-restore --verbosity normal
        
      - name: Create Deployment Meta
        run: .github/scripts/create_deployment.sh $PROJECT_ALIAS $APIKEY BuildNumber1234
        shell: bash
        
      - name: Zip Source Code
        run: git archive --format=zip HEAD -o /packed/sources.zip
        shell: bash
        
      - name: Post Zipped Artifect
        run: .github/scripts/upload_package.sh $PROJECT_ALIAS $deploymentId $APIKEY /packed/sources.zip
        shell: bash
        
      - name: Wait for deployment completed
        run: .github/scripts/get_deployment_status.sh $PROJECT_ALIAS $deploymentId $APIKEY
        shell: bash
      
